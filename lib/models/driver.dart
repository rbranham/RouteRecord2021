

class Driver{

  final String uid;
  final String name;
  // final String adminId;
  final String age;

  Driver({this.uid, this.name, this.age});
  // Driver({this.uid, this.name, this.adminId});

}